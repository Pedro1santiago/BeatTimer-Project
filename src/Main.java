public class Main {
    public static void main(String[] args) {
        // Apenas inicia o JavaFX — tudo será controlado dentro da classe JavaFX
        RelogioConfig.launch(RelogioConfig.class);
    }
}
